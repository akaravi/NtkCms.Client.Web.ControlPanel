app.controller("cmsMenueGroupCtrl", [
    "$scope", "$http", "ajax", 'rashaErManage', '$modal', '$modalStack', 'SweetAlert',
    function ($scope, $http, ajax, rashaErManage, $modal, $modalStack, sweetAlert) {
        var cmsMenueGroupCtrl = this;

        cmsMenueGroupCtrl.int = function(p) {
            
        }

    }
]);